
<div>
    <h3>Question ID<%= @question.id %></h3>
    <p><%= @question.content %></p>
    <h3>Answers</h3>
    <% if @question.answers.any? %>
        <p><%= render @question.answers %></p>
        <% else %>
        <p>No Answers yet.</p>
    <% end %>


<%= link_to 'Back', admin_path %>
